The frontend hasn't been built yet. Run `make build-frontend` (or `cd web && npm run build`)
to produce the real index.html etc. that vite writes into this directory; this placeholder
only exists so `go:embed dist` in embed.go has something to embed before that happens.
